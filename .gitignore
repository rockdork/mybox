# ============================================================
# mybox · 仓库文件约束（保持纯粹，只提交源码与必要资源）
# ============================================================

# ---------- 依赖（不提交，按 lockfile 还原） ----------
node_modules
/dist
dist-ssr
*.local

# ---------- 构建产物：Rust / Tauri ----------
# 4.3GB 的编译缓存，绝对不能进仓库
/src-tauri/target/
# src-tauri 内已用嵌套 .gitignore 忽略 /target 与 /gen/schemas，这里再兜底一次
**/*.rs.bk
*.pdb

# ---------- 打包产物（app / dmg / 安装包） ----------
*.app
*.dmg
*.AppImage
*.deb
*.rpm
*.msi
*.exe

# ---------- 本地运行数据（用户私有，勿上传） ----------
*.sqlite
*.sqlite-wal
*.sqlite-shm
workbench.json
settings.json
sync_state.json

# ---------- 日志 ----------
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ---------- 编辑器 / IDE ----------
.vscode/*
!.vscode/extensions.json
.idea
*.sw?

# ---------- 系统杂项 ----------
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
